# AIO mocking

The sandbox exposes `AIO.AddAddon`, `AIO.AddHandlers`, and `AIO.Handle`. They are safe in-memory stubs; calls never contact a server and Handle is traced. Configurable JSON response routing and state presets are not implemented in this slice.
